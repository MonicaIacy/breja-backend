// import express from "express"
// import debug from "debug"

// //import CreateOrderUseCase from "../../../domain/usecases/orders/order.create.usecase"

// const log: debug.IDebugger = debug("app: order-controller")

// class OrderController {
//   async createOrder(req: express.Request, res: express.Response) {
//     const order = await CreateOrderUseCase.execute(req.body)
//     res.status(201).send(order)
//   }
// }

// export default new OrderController()
